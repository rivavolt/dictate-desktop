# Project-local secrets, scoped to just the STT/LLM API keys this app needs — kept
# separate from the fleet monolith so loading the whole file in dev is safe (least
# privilege). Encrypted to andrei's personal age key, which is present on the dev
# hosts (riva/watts), so `sops -d` and the .envrc work there with no extra setup.
keys:
  - &personal age10q9h5a7eml9s4x9mqd64srw7u6us6zamhajjgp4nemfqe6tc5s0q4vvfs0
creation_rules:
  - path_regex: secrets\.yaml$
    key_groups:
      - age:
          - *personal
